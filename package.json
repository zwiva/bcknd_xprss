{
  "name": "express",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node --watch --env-file=environments/.env server.js",
    "start:qa": "node --watch --env-file=environments/.qa.env server.js",
    "start:prod": "node --watch --env-file=environments/.prod.env server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.1"
  }
}